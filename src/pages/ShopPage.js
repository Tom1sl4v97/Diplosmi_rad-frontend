import { useTranslation } from "react-i18next";

function ShopPage() {
  const { t: text } = useTranslation();
  
  return (
    <div>
      <h1>Shop Page</h1>
    </div>
  );
}

export default ShopPage;
